import { PageHeader } from "@/components/PageHeader";
import { Badge } from "@/components/ui/badge";
import { Card, CardContent } from "@/components/ui/card";
import { ShieldCheck } from "lucide-react";
import { trpc } from "@/lib/trpc";
export default function Audit() { const audit = trpc.governance.audit.useQuery({ limit: 100 }); return <div><PageHeader eyebrow="GOVERNANÇA" title="Trilha de auditoria" description="Todas as ações críticas de importação, reversão e administração são registradas com usuário, data, entidade e valores alterados."/><Card className="rounded-3xl border-[#ecd8bd] bg-white"><CardContent className="p-6"><div className="flex items-center gap-3"><span className="grid h-10 w-10 place-items-center rounded-2xl bg-[#fff0d5]"><ShieldCheck className="h-5 w-5 text-[#d06b15]"/></span><div><p className="font-bold text-[#34291f]">Eventos recentes</p><p className="text-xs text-[#7d6e60]">Acesso de leitura para perfis autorizados.</p></div></div><div className="mt-5 space-y-3">{audit.data?.length ? audit.data.map(event => <div key={event.id} className="flex flex-col justify-between gap-2 rounded-2xl border border-[#eee1d2] p-4 sm:flex-row sm:items-center"><div><p className="font-semibold text-[#3f3226]">{event.action}</p><p className="mt-1 text-xs text-[#7c6f62]">{event.entity}{event.entityId ? ` · ${event.entityId}` : ""}</p></div><Badge variant="outline" className="w-fit border-[#e5d1b3] text-[#70583f]">{new Date(event.createdAt).toLocaleString("pt-BR", { timeZone: "America/Belem" })}</Badge></div>) : <p className="rounded-2xl bg-[#fff8e8] p-5 text-sm text-[#776858]">Nenhum evento de auditoria registrado ainda.</p>}</div></CardContent></Card></div>; }
